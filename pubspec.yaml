name: photo_refractor
description: "A new Flutter project."

publish_to: "none"

version: 0.0.1+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_hooks: ^0.21.3+1
  go_router: ^17.0.1
  rxdart: ^0.28.0
  lucide_icons_flutter: ^3.1.9
  collection: ^1.19.1
  flutter_use: ^1.0.0
  fl_chart: ^1.1.1
  camera: ^0.11.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
